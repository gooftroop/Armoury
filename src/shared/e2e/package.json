{
    "name": "@armoury/e2e",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "default": "./dist/index.js"
        },
        "./*.js": {
            "types": "./dist/*.d.ts",
            "default": "./dist/*.js"
        }
    },
    "scripts": {
        "build": "ESBUILD_ENTRIES=src/index.ts,src/dockerSetup.ts,src/dockerSetupDbOnly.ts,src/e2eEnv.ts,src/helpers.ts,src/localstackConfig.ts,src/mockBroadcaster.ts,src/dbSchema.ts,src/syncHelpers.ts node --import=tsx --input-type=module -e \"import '@armoury/esbuild/service'\"",
        "generate:types": "tsc -p tsconfig.build.json",
        "lint": "eslint .",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "format": "prettier --write .",
        "format:check": "prettier --check ."
    },
    "dependencies": {
        "@aws-sdk/dsql-signer": "*"
    },
    "devDependencies": {
        "@armoury/esbuild": "*",
        "@armoury/eslint": "*",
        "@armoury/prettier": "*",
        "@armoury/typescript": "*",
        "@armoury/vitest": "*",
        "eslint": "^10.0.3",
        "prettier": "^3.0.0",
        "vitest": "^4.0.0"
    }
}

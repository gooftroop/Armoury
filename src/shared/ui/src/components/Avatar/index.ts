/**
 * @requirements
 * 1. Must re-export all Avatar component exports.
 * 2. Must use named exports only.
 */
export {
    Avatar,
    AvatarImage,
    AvatarFallback,
    avatarVariants,
    type AvatarProps,
    type AvatarImageProps,
    type AvatarFallbackProps,
} from './Avatar.web.js';

/**
 * @requirements
 * 1. Must export the routing config for locale-aware navigation.
 * 2. request.ts is consumed directly by Next.js via the plugin path — not exported here.
 */

export { routing } from './routing.js';

{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "buildCommand": "npm run copy:systems && next build",
    "build": {
        "env": {
            "NODE_OPTIONS": "--max-old-space-size=7168"
        }
    }
}

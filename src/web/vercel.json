{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "buildCommand": "npx turbo run build --filter=@armoury/web",
    "build": {
        "env": {
            "NODE_OPTIONS": "--max-old-space-size=7168"
        }
    }
}

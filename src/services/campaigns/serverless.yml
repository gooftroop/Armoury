service: armoury-campaigns

params:
    production:
        environment: production
    default:
        environment: sandbox

provider:
    name: aws
    runtime: nodejs22.x
    architecture: arm64
    stage: ${opt:stage, 'dev'}
    region: ${opt:region, 'us-east-1'}
    memorySize: 256
    timeout: 29
    environment:
        DSQL_ENDPOINT_PARAM: ${env:DSQL_ENDPOINT_PARAM, '/armoury/${param:environment}/campaigns/dsql-cluster-endpoint'}
        NODE_OPTIONS: '--enable-source-maps'
        SENTRY_DSN: ${ssm:/armoury/${param:environment}/campaigns/sentry-dsn}
        SENTRY_RELEASE: ${env:SENTRY_RELEASE, ''}
        SENTRY_ENVIRONMENT: ${self:provider.stage}
        DB_SCHEMA: ${env:DB_SCHEMA, 'public'}
        ALLOWED_ORIGIN: ${self:custom.allowedOrigin}
    iam:
        role:
            statements:
                - Effect: Allow
                  Action:
                      - dsql:DbConnectAdmin
                  Resource: 'arn:aws:dsql:${self:provider.region}:${aws:accountId}:cluster/*'
                - Effect: Allow
                  Action:
                      - ssm:GetParameter
                  Resource: 'arn:aws:ssm:${self:provider.region}:${aws:accountId}:parameter/armoury/${param:environment}/*'
    httpApi:
        authorizers:
            auth0Jwt:
                type: jwt
                identitySource: $request.header.Authorization
                issuerUrl: https://${env:AUTH0_DOMAIN}/
                audience:
                    - ${env:AUTH0_AUDIENCE}
        cors:
            allowedOrigins:
                - ${self:custom.allowedOrigin}
            allowedHeaders:
                - Content-Type
                - Authorization
            allowedMethods:
                - GET
                - POST
                - PUT
                - PATCH
                - DELETE
                - OPTIONS
    logs:
        httpApi: true

build:
    esbuild:
        bundle: true
        minify: true
        sourcemap:
            enabled: true
            setNodeOptions: false
        target: es2022
        platform: node
        format: esm
        mainFields:
            - module
            - main
        banner:
            js: "import { createRequire } from 'module'; const require = createRequire(import.meta.url);"
        external:
            - '@aws-sdk/dsql-signer'
            - '@aws-sdk/client-ssm'

plugins:
    - serverless-offline

custom:
    allowedOrigin: ${env:ALLOWED_ORIGIN, '*'}
    serverless-offline:
        httpPort: 3010
        lambdaPort: 3012
        noPrependStageInUrl: true

functions:
    campaigns:
        handler: src/handler.handler
        events:
            - httpApi:
                  path: /campaigns
                  method: ANY
                  authorizer:
                      name: auth0Jwt
            - httpApi:
                  path: /campaigns/{id}
                  method: ANY
                  authorizer:
                      name: auth0Jwt
            - httpApi:
                  path: /campaigns/{id}/participants
                  method: ANY
                  authorizer:
                      name: auth0Jwt
            - httpApi:
                  path: /campaigns/{id}/participants/{pid}
                  method: ANY
                  authorizer:
                      name: auth0Jwt

resources:
    Resources:
        CampaignsLogGroup:
            Type: AWS::Logs::LogGroup
            Properties:
                LogGroupName: !Sub '/aws/lambda/armoury-campaigns-${self:provider.stage}-campaigns'
                RetentionInDays: 14
        CampaignsLogDrainSubscriptionFilter:
            Type: AWS::Logs::SubscriptionFilter
            DependsOn: CampaignsLogGroup
            Properties:
                LogGroupName: !Sub '/aws/lambda/armoury-campaigns-${self:provider.stage}-campaigns'
                FilterPattern: ''
                DestinationArn: !Sub '{{resolve:ssm:/armoury/${param:environment}/log-drain/lambda-arn}}'
